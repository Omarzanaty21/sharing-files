@model FileSharing.Models.UserViewModel
@{
    ViewData["Title"] = "Info";
}


<!-- Start Contact -->
    <section id="mu-contact">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="mu-contact-area" style="padding-top: 0px">
                        <!-- Title -->
                        <div class="row">
                            <div class="col-md-12">
                                <div class="mu-title">
                                    <h2>@Localizer["LogInUser"]</h2>
                                </div>
                            </div>
                        </div>
                            <!-- Start Contact Content -->
                        <div class="mu-contact-content">
                            <div class="row">

                                <div class="col-md-12">
                                    <div class="mu-contact-form-area">
                                        <div id="form-messages"></div>
                                        <form method="post" class="mu-contact-form" asp-action="Info">
                                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                             <div class="form-group">  
                                                <span class="fa fa-envelope mu-contact-icon"></span>              
                                                <input asp-for="FirstName" type="text" class="form-control" placeholder="@Localizer["FirstName"]" required>
                                                <span asp-validation-for="FirstName"></span>
                                            </div>  

                                             <div class="form-group">  
                                                <span class="fa fa-envelope mu-contact-icon"></span>              
                                                <input asp-for="LastName" type="text" class="form-control" placeholder="@Localizer["LastName"]" required>
                                                <span asp-validation-for="LastName"></span>
                                            </div> 
                                            <div class="form-group">  
                                                <span class="fa fa-user mu-contact-icon"></span>              
                                                <input asp-for="Email" type="email" class="form-control" placeholder="@Localizer["Email"]" id="Email" name="Email" required readonly>
                                                <span asp-validation-for="Email"></span>
                                            </div>
                                              
                                            <button type="submit" class="mu-send-msg-btn"><span>@Localizer["SaveChanges"]</span></button>
                                        </form>
                                    </div>
                                </div>

                            </div>
                        </div>
                        <!-- End Contact Content -->
                    </div>
                </div>
            </div>
        </div>
    </section>
		<!-- End Contact -->


<h2>@Localizer["Manage"]</h2>
<a class="btn btn-outline-primary">Change Password</a>

@section Scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
